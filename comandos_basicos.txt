mkdir: criar pasta
git init: cria um diretorio .git 

git status 

//1 -untracked files
 nao faz parte do controle de versao do git

//2 - unstage estado - quando o arquivo esta pronto para ser enviado
git add nomedoarquivo
git add nome do aruivo

//3 - commit - ARQUIVOS ESTA FAZENDO PARTE DO CONTROLE DE VERSAO
git commit -m "nome do commit"


git log (verificar o log do commit)

!---------------------------COMANDOS PROMPT-----------------------!
(Fechar um aplicativo em execuão)
taskkill /f /im nomeDoPrograma.exe

(sfc) verifica erros no windos
sfc /scannow

SHUTDOWN(DELIGA O COMPUTADO
shutdown -s -t tempo em segundos
shutdown -a cancelar o comando

(type) visualizar arquivos de texto

type nomedoarquivo.txt |more (opcional)

netstat verificar portas no pc
netstat -an |more



